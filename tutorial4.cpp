#include <iostream>
#include <string>
#include <cstring>

using namespace std;
int main()
{
  int k = 4;
  double m = 4.3;
  // 1. transform k and m to std::string
  // 2. create std::string  testStr with "I love c++" content
  // 3. print testStr length on the screen
  // 4. save testStr as c-string (const char*) called testCStr
  // 5. print its content on the screen strlen()
  // 6. sort testStr 
  // 7. print 2nd element of the testStr
  // 8. find occurence of +
  // 9. replace "++" by "--"
  // 10. concatenate  testStr with "how are you" string
  return 0;
}
